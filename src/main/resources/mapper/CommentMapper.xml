<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pif.mapper.CommentMapper">

	<insert id="registerComment" parameterType="com.example.pif.dto.CommentVO">
		insert into 
			comment (c_id, c_getnum, c_content) 
		values
			(#{c_id}, #{c_getnum}, #{c_content})
	</insert>
	
	<select id="getCommentList">
		select * from comment where c_getnum = #{c_getnum}
	</select>
	
    <select id="getCount">
		select count(*) from comment where c_getnum = #{c_getnum}
	</select>

    <update id="updateComment">
		update 
			comment
		set
			c_content=#{c_content}
		where
			c_num = #{c_num}
	</update>

    <delete id="deleteComment">
		delete from comment where c_num = #{c_num}
	</delete>

</mapper>